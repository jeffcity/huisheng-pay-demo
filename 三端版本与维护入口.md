# 三端版本与唯一维护入口

核对日期：2026-09-25。此为迁移基线，不是锁定后续源码的版本号。

| 端 | 核对的 GitHub main 提交 | 统一仓库源码 | 核对结果 |
|---|---|---|---|
| 平台 | 204e70e37bab4d9a47cd70f60ebfd8cd4cb97fa7 | src/、public/legacy/sources/ | 已覆盖该提交；另保留当前任务删除登录帮助按钮及统一壳修改 |
| 租户 | e6ec8d7c9120ec0a028d9a28862b207d1e203a08 | surfaces/tenant/ | 已从最新工作副本导入商户管理、2FA 登录及账户补单限制；保留单文件读源/路由适配 |
| 商户 | bef28343895b4936d29a3e486bcde228c61ed037 | surfaces/merchant/demo.html | 源码 Git blob 哈希 83e483e967c3c505df526a2e64665d6159cf1528，与远端最新 main 一致，包含通知机器人与消息通知配置 |

## 唯一维护规则

- 后续三端 Demo 只维护本仓库（GitHub 仓库 huisheng-pay-demo，原名 huisheng-pay-platform），只交付生成的一个 index.html。本地继续使用原「平台端」目录，不创建第二份维护副本。
- 原 GitHub项目 下租户端、商户端及 raw/_delivery/huisheng-domain-20260814 下三份旧工作副本均停止维护，仅保留历史；不要继续改旧副本、从旧副本覆盖或向旧端仓库双写。
- 三端业务源码、导航、测试和打包脚本均在本仓库内。没有跨仓库读文件、子模块或运行时多端口依赖；未选中的端不执行脚本。
- 旧独立在线站点未远端关闭，不能作为当前统一版入口。本次没有自动推送或变更旧站部署。

## 验证与发布

- npm run check 校验平台、租户和商户功能基线及每个打包源与当前源码一致；dist 只允许 index.html。
- npm run smoke 直接验证单文件、子目录托管、三端切换/卸载、平台钱包、租户商户配置/资金调整、两条 2FA 登录、账户补单限制、商户通知配置。
- 验证新增功能是否存在，而非只判断页面能打开。最新租户的原商户交互测试在同步前先重现了“业务视图仍存在”的失败。
- GitHub Pages 使用本仓库 workflow 发布；CI 在 npm ci、check 和浏览器回归通过后才上传 dist。
- 已只读核对平台仓库 Pages 为 workflow 模式，既有部署成功。新版本仍需推送后的 Actions 与实际在线页面验证，不能用本地检查代替线上结果。
- 已完成脱离旧目录、无既有 node_modules 的全新安装演练：npm ci → build → check，24 项测试全部通过。根文件可从同一份源码重新生成，不依赖迁移时的临时目录。

需求文档保留在原资料目录；这里只合并可执行 Demo 源与必要测试，不复制需求资料形成另一套维护版本。
